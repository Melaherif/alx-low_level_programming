#include "main.h"
/**
*swap_int- a function swap the para b on a
*@a:is para swap to b
*@b:is para swap to a
*Return:empty
**/
void swap_int(int *a, int *b)
{
int temp;
*a = temp;
*a = *b;
*b = temp;
}
